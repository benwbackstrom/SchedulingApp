export class Appointment {
    firstName!: string;
    lastName!: string;
    email!: string;
    //For now I have these as strings, but they may be better as other variables
    location!: string;
    date!: string;
    time!: string;
}
